import 'package:flutter/material.dart';

class PinkRouterWidget extends StatefulWidget {
  final Navigator child;

  const PinkRouterWidget({
    Key key,
    this.child,
  }) : super(key: key);

  @override
  _PinkRouterWidgetState createState() => _PinkRouterWidgetState();
}

class _PinkRouterWidgetState extends State<PinkRouterWidget> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
